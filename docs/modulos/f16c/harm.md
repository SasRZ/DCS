# F-16C · HARM en modo POS y HAD

Este es un documento de ejemplo. Sustitúyelo por tu propio manual o apunte.

Sirve para mostrar cómo un mismo archivo puede aparecer en dos categorías
a la vez (Módulos → F-16C, y Tipos de misión → SEAD) sin duplicar el archivo:
basta con listar las dos rutas en el campo "refs" de su ficha en
`data/documentos.json`.
